# Required — https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Tools — each one works independently; if missing,
# the matching tool returns a handled error (the rest of the agent keeps working)
OPENWEATHER_API_KEY=
TAVILY_API_KEY=
RESEND_API_KEY=
RESEND_FROM_EMAIL=onboarding@resend.dev

MODEL_NAME=claude-sonnet-5
MAX_AGENT_ROUNDS=6
TOOL_TIMEOUT_SECONDS=8

CORS_ORIGINS=http://localhost:5173
